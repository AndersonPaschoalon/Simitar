# variaveis para definir: capture_name, duration(exec_time) interface ethernet(eth_if) e binario(bin_file)
# 1. executar tcpdump na interface eth_if capturando pcap capture_name, e salvando no diretorio ./pcaps/
# 2. executar binario do simitar bin_file
# 3. depois de exec_time + 1s, matar o tcpdump




